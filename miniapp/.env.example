# Same bot token as the existing ProqnozAI bot (BotFather) — this is the
# ONLY thing shared with the old service; used to validate Telegram initData.
TELEGRAM_TOKEN=123456:replace-me

# Optional
MINIAPP_DB_DIR=.
PORT=8000
